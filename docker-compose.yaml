version: "3.9"

services:
  web:
    build: .
    container_name: agentic_web
    ports:
      - "8000:8000"
    volumes:
      - .:/app  # sync local code for hot reload
    environment:
      - PYTHONUNBUFFERED=1
